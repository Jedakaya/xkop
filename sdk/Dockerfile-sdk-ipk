# Prepared OpenWrt SDK for ipk builds - the branch the client fleet runs on.
# See sdk/Dockerfile-sdk-apk for why the slow part lives in a separate image.
FROM openwrt/sdk:x86-64-24.10.8

LABEL org.opencontainers.image.source="https://github.com/Jedakaya/xkop"

WORKDIR /builder

RUN [ -x ./setup.sh ] && ./setup.sh || true

RUN ./scripts/feeds update -a \
    && mkdir -p /builder/package/feeds/utilities/
